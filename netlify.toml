[build]
  command = "npm install && npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions."claude"]
  timeout = 60

[functions."generate-resume"]
  timeout = 60

[[redirects]]
  from = "/auth/callback"
  to = "/.netlify/functions/gmail-auth-callback"
  status = 200
